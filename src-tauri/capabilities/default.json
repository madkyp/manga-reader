{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos de la aplicación manga reader",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "core:tray:default",
    "dialog:default",
    "opener:default"
  ]
}
